# DeployDock 요구사항

작성일: 2026-09-16 · 상태: 재구축을 위한 요구사항 초안

로그인과 기존 계정은 유지하고, 그 외 기능은 이 요구사항을 기준으로 다시 설계한다.
웹 배포와 배치 배포를 구분하고, 각각 **사용자가 사용할 수 있는 기능**과 **시스템이 보장해야 하는 동작**을 나눈다.
이 문서는 목표 동작을 정의하며, 현재 코드가 이를 구현했다고 의미하지 않는다.

- **요청**: 사용자가 명시한 요구사항.
- **구체화**: 요청한 기능을 일관되게 동작시키고 테스트하기 위해 도출한 조건. 검토 대상이다.
- **제안**: 추가하면 좋은 기능 또는 보장. 채택 여부를 결정해야 한다.
- **미정**: 선택 가능한 정책이 남은 항목. 구현에서 임의로 확정하지 않는다.

## 1. 용어와 범위

| 용어 | 의미 |
|---|---|
| 애플리케이션 | 배포 큐와 현재 배포 버전을 관리하는 단위. 대상 클러스터·namespace와 연결된다. |
| 배포 설정 | 이미지, 실행 config, 자원, Pod 개수, 전략 등 배포에 필요한 사용자 설정. |
| 설정 리비전(설정 버전) | 저장된 배포 설정의 변경 버전. 내용이 달라진 상태로 저장할 때 증가하며, Pod 개수만 변경해도 증가한다. 예: `r10`, `r11`. |
| 배포 버전 | 실제로 실행할 이미지와 Pod 실행 설정의 조합을 식별하는 버전. 이미지·환경변수·자원 등 실행 내용이 달라지면 새 버전으로 구분한다. Pod 개수와 배포 전략은 포함하지 않는다. 예: `v3`, `v4`. |
| 작업 ID | 배포·수동 스케일 변경 등 개별 적용 요청의 식별자. 같은 버전에 대한 새로운 요청에도 새 ID를 부여한다. 동일 요청의 재시도·장애 인계는 기존 ID를 유지한다. |
| 배포 요청(Run) | 특정 설정 리비전의 실행 내용을 배포하는 작업. 작업 ID로 식별하며, 설정 저장과 별도 작업이다. 요청이 접수됐다는 사실이 해당 버전의 서비스 시작을 의미하지 않는다. |
| 스케일 변경 | 배포 버전을 유지하면서 원하는 Pod 개수를 변경하는 작업. 수동 변경은 설정 저장 후 별도 적용 요청으로 수행하고 작업 이력을 남긴다. |
| 배포 큐 | 애플리케이션별 배포 요청과 수동 스케일 변경 요청을 접수 순서대로 처리하는 대기열. 같은 앱의 두 작업은 동시에 적용하지 않는다. |
| 배치 실행 | 특정 버전으로 배치 작업을 한 번 수행하는 것. 배치 배포와 별도 작업이다. |
| 그루핑 배포 | 하나의 배치 배포 Run에서 두 개 이상의 배치 대상을 같은 설정 리비전으로 함께 활성화하는 방식. |
| 개별 배포 | 하나의 배치 배포 Run에서 단일 배치 대상만 활성화하는 방식. |
| 배포 이력 | 어떤 사용자가 언제 어떤 버전을 배포했고, 어떤 결과가 났는지에 대한 기록. 웹·배치 모두 해당한다. |
| 배치 실행 이력 | 배치 작업별 실행 주체, 사용 버전, 실행 시간과 결과에 대한 기록. |
| orphan | 처리 주체를 잃은 요청이나, 대응하는 관리 기록을 확인할 수 없는 실제 리소스 등 대조·복구가 필요한 대상. |

구현 순서는 **웹 배포 → 배포 이력 → 배치 배포 → 배치 실행 이력 → 권한 다듬기**를 기준으로 한다.
큐 정합성·장애 복구·orphan 처리는 각 기능의 기본 조건에 포함한다. 권한 화면을 나중에 다듬더라도 서버의 접근 검사를 생략하지 않는다.
큐 제품, 데이터베이스, 실행기 구성 등 구현 기술은 별도 설계에서 결정한다.

**현재 구현 단위는 단일 Gradle 모듈·단일 Spring Boot 애플리케이션·단일 실행 JAR로 한다.**
인증, API, 배포 처리, 큐 처리, 이력, 복구는 같은 모듈 안에서 패키지로 구분한다.
API와 백그라운드 제어 작업은 같은 애플리케이션 프로세스에서 실행하고, HTTP 요청은 배포 완료를 기다리지 않는다.
HA 구성과 장애 격리 범위를 정한 뒤 필요에 따라 실행 역할과 빌드 모듈을 분리한다.
요청 영속성, 앱별 순차 처리, 재시작 후 재개, orphan 처리 요구사항은 이 결정과 별개로 유지한다.
단일 프로세스 단계에서는 다른 인스턴스로의 자동 인계와 노드 장애 HA를 충족했다고 판단하지 않는다.

### 1.1 설정 리비전·배포 버전·작업 ID의 변경 규칙

아래는 DeployDock의 버전 규칙이다. Kubernetes 내부 revision 번호와 독립적으로 관리한다.
**설정 저장은 실제 적용과 별개이며, 저장만으로 배포하거나 Pod 개수를 변경하지 않는다.**

| 변경 또는 요청 | 설정 리비전 | 배포 버전 | 적용 동작 |
|---|---|---|---|
| 이미지 변경 | 증가 | 변경된 실행 내용에 해당하는 버전 | 선택한 전략으로 배포 |
| 환경변수·ConfigMap·Secret 실행 내용 변경 | 증가 | 변경된 실행 내용에 해당하는 버전 | 고정된 config로 배포 |
| CPU·메모리, 실행 명령, readiness 변경 | 증가 | 변경된 실행 내용에 해당하는 버전 | 변경된 Pod 설정으로 배포 |
| Pod 개수만 3 → 5로 변경 | 증가 | 유지 | 현재 버전의 Pod 증설 |
| 배포 전략만 변경하여 저장 | 증가 | 유지 | 저장만으로 실행하지 않고 이후 배포 요청에 사용 |
| 동일한 설정을 다시 저장 | 유지 | 유지 | 변경 없음 |
| 동일한 배포 버전의 재배포 요청 | 유지 | 유지 | 새 작업 ID로 배포 처리 |
| 동일 요청의 재시도 또는 장애 인계 | 유지 | 유지 | 기존 작업 ID로 이어서 처리 |

이미지는 태그 문자열뿐 아니라 실제 사용할 이미지 내용으로 구분한다. 같은 `latest` 태그라도 이미지 내용이 바뀌면 다른 배포 버전이다.
ConfigMap·Secret도 참조 이름뿐 아니라 실제 실행에 사용할 내용을 고정한다. 민감한 값 자체를 버전 목록이나 이력에 노출하지 않는다.
배포 버전은 저장된 실행 내용의 식별자이므로, 설정을 저장했다고 현재 서비스 버전이 바뀌지는 않는다.

### 1.2 스케일 변경과 롤백 예시

1. 설정 `r10`, 배포 버전 `v3`, 원하는 Pod 개수 3개로 서비스한다.
2. Pod 개수를 5개로 수정하여 저장하면 설정 `r11`이 된다. 아직 실제 Pod 개수는 바뀌지 않는다.
3. 스케일 변경을 신청하면 새 작업 ID로 앱의 큐에 들어간다. 적용 후 `v3` Pod 5개로 서비스한다.
4. 이후 `v4` 배포가 실패하여 `v3`으로 롤백하면 이미지와 Pod 실행 설정은 `v3`으로 복원하고 원하는 Pod 개수는 5개를 유지한다.

**배포 버전 롤백은 과거 설정 리비전 전체를 덮어쓰는 작업이 아니다.** 현재 적용된 목표 Pod 개수는 유지하며, 개수를 되돌리려면 별도의 스케일 변경을 요청한다.
이력에는 작업 ID, 작업 종류, 참조한 설정 리비전, 대상 배포 버전, 변경 전후 Pod 개수와 결과를 구분해서 기록한다.
배치 작업의 실행 동시성은 웹 Pod 개수와 별개이며, 실행 중인 배치 보호 규칙을 따른다.

향후 HPA를 지원하면 자동 증감은 설정 리비전을 증가시키지 않고 자동 스케일 이벤트로 기록한다.
사용자가 저장하는 목표 설정과 HPA가 조정하는 실제 규모를 구분하며, HPA 지원 자체와 수동 변경과의 조정 정책은 별도로 결정한다.

## 2. 공통 요구사항

### 2.1 사용자가 사용할 수 있는 기능

| ID | 구분 | 기능 | 사용자가 확인할 결과 |
|---|---|---|---|
| CU-01 | 요청 | 기존 계정으로 로그인 | 기존 계정과 로그인 기능을 계속 사용할 수 있다. |
| CU-02 | 요청 | namespace·애플리케이션·배포 설정 조회 | 접근 가능한 대상과 저장된 설정을 화면에서 찾을 수 있다. |
| CU-03 | 요청 | 여러 애플리케이션 구성 | 앱 A와 앱 B를 각각 등록하고 설정·배포 요청을 관리할 수 있다. |
| CU-04 | 요청 | 설정 저장과 배포 신청을 별도로 수행 | 설정을 저장해 둔 뒤, 선택한 설정으로 명시적으로 배포를 신청할 수 있다. |
| CU-05 | 요청 | 큐 조회 및 대기 요청 취소 | 앱별 진행 요청과 대기 요청을 확인하고, 대기 중인 요청을 취소할 수 있다. |
| CU-06 | 구체화 | 배포 진행·취소·복구 상태 조회 | 요청 접수, 실행, 취소 접수, 복구 진행, 최종 결과를 구분할 수 있다. |
| CU-07 | 요청 | 배포 이력 조회 | 신청자, 신청·시작·종료 시각, 대상 앱, 배포 버전, 성공·실패·취소 결과를 확인할 수 있다. |
| CU-08 | 구체화 | 장애 원인과 관측 시각 확인 | 실패 이유, 마지막 상태 확인 시각, 관측 불가 또는 운영자 조치 필요 여부를 확인할 수 있다. |
| CU-09 | 제안 | 이력 검색·필터·페이지 조회 | 앱·기간·신청자·결과로 필요한 기록을 찾고, 기록 증가 시에도 목록을 나눠 볼 수 있다. |
| CU-10 | 제안 | 권한에 따른 복구 조치 | 권한이 있는 운영자가 근거를 확인하고 재개·리소스 채택·정리를 각각 요청할 수 있다. |

### 2.2 시스템이 보장해야 하는 동작

| ID | 구분 | 보장 | 검증 기준 |
|---|---|---|---|
| CS-01 | 요청 | 다중 사용자 요청 처리 | 여러 사용자가 동시에 신청해도 접수된 요청이 누락되거나 서로 덮어써지지 않는다. |
| CS-02 | 요청 | 애플리케이션별 순차 배포 | 같은 앱에는 논리적으로 하나의 배포만 진행된다. 나머지는 큐에 쌓이고 선행 배포 종료 후 처리된다. |
| CS-03 | 구체화 | 일관된 큐 순서 | 영속적으로 접수한 순서로 처리한다. 동시 접수에도 순서가 정해지고, 취소된 대기 요청은 실행하지 않는다. |
| CS-04 | 요청 | 서로 다른 앱의 병렬 배포 | 앱 A의 배포·대기·복구가 앱 B의 큐를 논리적으로 잠그지 않는다. 실제 동시 처리량은 가용 자원의 영향을 받는다. |
| CS-05 | 구체화 | 중복 신청·취소에 대한 일관성 | 같은 요청 식별자로 재시도하면 기존 결과를 반환한다. 같은 내용의 의도적인 새 신청은 별도 요청으로 구분한다. |
| CS-06 | 구체화 | 배포 설정 고정 | 접수한 Run이 사용하는 설정을 고정한다. 이후 설정 편집으로 대기·진행 중인 Run의 내용이 바뀌지 않는다. |
| CS-07 | 구체화 | 대기 취소와 실행 시작의 경쟁 처리 | 취소가 먼저 확정되면 실행하지 않는다. 실행 시작이 먼저 확정되면 진행 중 취소 정책을 적용하고 실제 상태를 반환한다. |
| CS-08 | 요청 | 단일 컴포넌트 장애에 대한 인계·복구 | API·실행기·복구 담당 프로세스가 종료되어도 접수한 요청을 보존하고 다른 인스턴스가 이어서 처리할 수 있다. 저장소를 포함한 장애 범위는 D-09에서 확정한다. |
| CS-09 | 구체화 | 이전 처리자의 늦은 변경 차단 | 인계 후 복귀한 이전 처리자의 명령이 새 처리자의 배포 버전·트래픽·최종 결과를 덮어쓰지 못한다. |
| CS-10 | 구체화 | 외부 작업 결과 유실 복구 | Kubernetes 작업 직후 프로세스가 종료돼도 실제 리소스를 대조하여 같은 Run을 재개한다. 결과를 모른다는 이유만으로 새 배포를 중복 생성하지 않는다. |
| CS-11 | 요청 | orphan 탐지와 처리 | 기록에서 실제 리소스를, 실제 리소스에서 관리 기록을 양방향으로 대조한다. 처리자 유실, 부분 생성, 기록 없는 리소스를 탐지한다. |
| CS-12 | 구체화 | 근거에 따른 복구·정리 | 소유권과 실제 상태가 확인되면 인계·재사용·보완한다. 불명확하면 격리하고 근거를 표시한다. 이름만 같은 새 리소스를 이전 리소스로 취급하지 않는다. |
| CS-13 | 구체화 | 장애·정리 중 기존 워크로드 보호 | 현재 서비스 버전, 롤백 대상, 실행 중인 배치와 공유 DB·Secret·저장소를 임의 삭제하지 않는다. 정리 작업의 재시작·중복 실행도 안전해야 한다. |
| CS-14 | 구체화 | 관측 불가와 실행 실패 구분 | API timeout·권한 오류를 리소스 삭제나 배포 실패로 단정하지 않는다. 소유권·상태를 확인할 수 없으면 관련 변경과 정리를 보류한다. |
| CS-15 | 구체화 | 복원 후 실제 상태 대조 | 백업 복원으로 기록이 실제 클러스터보다 과거가 된 경우 대조 후 재개한다. 과거 대기·진행 상태를 그대로 재실행하지 않는다. |
| CS-16 | 구체화 | 일관된 이력과 행위자 기록 | 사용자 신원은 인증 정보에서 얻는다. 상태 변경·취소·자동 인계·복구 조치를 기록하고, 복구 성공으로 원래 실패 기록을 덮어쓰지 않는다. |
| CS-17 | 구체화 | 서버 접근 검사와 민감 정보 보호 | 대상 앱·namespace에 대한 조회·변경 권한을 서버에서 검사한다. Secret 값이 일반 목록·이력·오류에 노출되지 않는다. |
| CS-18 | 구체화 | 오래 걸리는 배포와 웹 응답 분리 | 신청 API는 배포 완료까지 기다리지 않고 접수 결과를 반환한다. 목록 조회에서 매번 클러스터 전체를 탐색하지 않으며 오래된 관측값임을 표시한다. 수치 목표는 D-10에서 정한다. |

## 3. 웹 배포

### 3.1 사용자가 사용할 수 있는 기능

| ID | 구분 | 기능 | 사용자가 확인할 결과 |
|---|---|---|---|
| WU-01 | 요청 | 실제 이미지와 config를 사용하는 배포 설정 | 이미지와 애플리케이션 실행에 필요한 설정을 저장하고 해당 설정으로 배포할 수 있다. |
| WU-02 | 구체화 | 실행 조건 설정 | 이미지 접근 정보, 포트, 환경변수, ConfigMap·Secret 참조, 자원 요청·제한, readiness 조건을 설정할 수 있다. |
| WU-03 | 요청 | 배포 전략 선택 | 롤링(rollout), 블루그린, 카나리 중 하나를 선택할 수 있다. 서버 API는 `ROLLING`, `BLUE_GREEN`, `CANARY` 값을 검증한다. |
| WU-08 | 요청 | Temporal 오케스트레이션 선택 | 애플리케이션 등록 시 로컬 처리 또는 Temporal 기반 처리(`LOCAL`, `TEMPORAL`)를 선택할 수 있다. |
| WU-04 | 요청 | 진행 중 배포 취소 | 취소를 신청하고 이전 정상 버전으로 복구되는 진행 과정과 결과를 확인할 수 있다. |
| WU-05 | 요청 | 실패 원인 확인 | 블루그린 자원 부족 등 실패 원인을 해당 Run의 상세·이력에서 확인할 수 있다. |
| WU-06 | 구체화 | 현재 버전과 배포 이력 비교 | 현재 서비스 버전, 배포 대상 버전, 복구 대상 버전을 구분하고 각 Run이 사용한 설정을 확인할 수 있다. |
| WU-07 | 제안 | 이전 정상 버전 재배포 | 완료된 과거 설정을 선택해 새 배포 요청을 만들 수 있다. 진행 중 취소와 별도 기능이다. |

### 3.2 시스템이 보장해야 하는 동작

| ID | 구분 | 보장 | 검증 기준 |
|---|---|---|---|
| WS-01 | 요청 | 롤링 배포 | 이전 인스턴스를 단계적으로 새 버전으로 교체하고 정상 준비 상태를 확인한다. 허용되는 가용성·추가 자원 기준은 설정으로 명시한다. |
| WS-02 | 요청 | 블루그린 배포 | 새 버전을 별도로 준비하고 정상 상태를 확인한 후 서비스 트래픽을 전환한다. 새 버전 준비 실패 시 기존 서비스 버전을 유지한다. |
| WS-03 | 요청 | 카나리 배포 | 새 버전을 일부에 노출하고 단계적으로 확대한다. 현재 API 계약은 전략 선택을 고정하며, 실제 노출 방식과 단계별 승격 조건은 실행기 구현에서 D-08 기준을 따른다. |
| WS-04 | 구체화 | 실제 제공 상태를 기준으로 완료 판정 | 리소스 생성 요청 성공만으로 배포 성공을 기록하지 않는다. 전략별 준비 상태·전환 결과를 확인하고 실제 HTTP 요청으로 검증 가능해야 한다. |
| WS-05 | 구체화 | 이미지·config의 재현 가능성 | 변경 가능한 이미지 태그와 config도 Run에 적용할 내용을 식별·고정할 수 있어야 한다. 롤백 시 이전 이미지와 Pod 실행 설정을 복원하며, 현재 적용된 목표 Pod 개수는 유지한다. |
| WS-06 | 요청 | 진행 취소 시 이전 정상 버전 복구 | 취소 접수와 취소 완료를 구분한다. 이전 버전의 준비 상태와 서비스 연결이 확인된 뒤 취소 완료로 처리한다. |
| WS-07 | 구체화 | 첫 배포 취소 | 이전 버전이 없으면 미배포 상태로 되돌린다. 해당 Run의 생성 리소스만 정리하고 외부 공유 자원은 유지한다. |
| WS-08 | 구체화 | 복구 실패 시 후속 배포 차단 | 롤백 timeout·실패를 취소 완료로 표시하지 않는다. 복구 필요 상태와 원인을 기록하고, 안전한 상태가 확인될 때까지 같은 앱의 다음 Run을 시작하지 않는다. |
| WS-09 | 요청 | 자원 부족 원인 설명 | 관측된 CPU·메모리 부족, quota, 스케줄링 제약 등을 구분해 표시한다. 근거가 없으면 자원 부족으로 추측하지 않고 확인 불가를 표시한다. |
| WS-10 | 제안 | 실패 시 자동 롤백 | 배포 실패 시 이전 정상 버전으로 자동 복구한다. 실패 판정·대기 시간·자동 복구 범위는 D-11에서 결정하며, 배포 결과와 복구 결과를 별도로 기록한다. |

## 4. 배치 배포와 실행

### 4.1 사용자가 사용할 수 있는 기능

| ID | 구분 | 기능 | 사용자가 확인할 결과 |
|---|---|---|---|
| BU-01 | 요청 | 배치 버전 배포 | 배치 설정을 저장하고 별도의 Run으로 새 버전을 활성화할 수 있다. |
| BU-02 | 구체화 | 배치 실행 설정 | 이미지·config 외에 실행 명령·인자·자원 조건을 설정할 수 있다. timeout·재시도·중복 실행 정책은 별도 확정한다. |
| BU-03 | 요청 | 그루핑 배포와 개별 배포 | 배치 설정 저장 시 `GROUPED` 또는 `INDIVIDUAL` 배포 모드를 선택할 수 있다. `GROUPED`는 두 개 이상의 배치 대상, `INDIVIDUAL`은 최대 한 개의 배치 대상을 허용한다. |
| BU-04 | 구체화 | 수동 실행과 스케줄 실행 | 사용자가 직접 작업을 실행하거나 선택한 스케줄러로 실행할 수 있다. 스케줄러는 Jenkins·Airflow·CronJob 중 아직 정하지 않았다. |
| BU-05 | 구체화 | 활성 버전과 실행 중 버전 확인 | 현재 신규 실행에 적용할 버전과 이미 실행 중인 각 작업의 버전을 구분할 수 있다. |
| BU-06 | 요청 | 배치 배포 이력 조회 | 누가 언제 어떤 배치 버전을 배포했는지와 배포 결과를 확인할 수 있다. |
| BU-07 | 요청 | 배치 실행 이력 조회 | 수동 실행인지 스케줄러 실행인지 구분하고, 수동 실행자·실행 버전·시작/종료 시각·결과를 확인할 수 있다. |
| BU-08 | 요청 | 실행 이력 보관 옵션 설정 | 기본 최신 10건 보관과, 옵션 활성화 시 한 달 보관을 사용할 수 있다. 세부 적용 기준은 아래 정책 표에서 정한다. |
| BU-09 | 제안 | 실행 로그·실패 상세 조회 | 개별 실행의 실패 원인과 로그를 볼 수 있다. 로그의 보관 범위는 실행 이력과 별도로 결정한다. |

### 4.2 시스템이 보장해야 하는 동작

| ID | 구분 | 보장 | 검증 기준 |
|---|---|---|---|
| BS-01 | 구체화 | 배포와 작업 실행 분리 | 배치 배포는 신규 실행에 사용할 활성 버전을 변경한다. 배포했다는 이유만으로 배치 작업을 자동 실행하지 않는다. |
| BS-02 | 요청 | 실행 중인 이전 버전 보호 | v1 작업이 실행 중일 때 v2를 배포해도 해당 작업은 v1 이미지·config로 완료된다. 배포로 인해 작업을 종료·재시작·실패시키지 않는다. |
| BS-03 | 요청 | 활성화 이후 새 버전 실행 | v2 활성화 이후 새로 요청한 실행은 v2를 사용한다. 전환 전에 대기 중이던 실행의 버전 선택은 D-02에서 결정한다. |
| BS-04 | 구체화 | 배포 큐와 실행 동시성 분리 | 같은 배치 앱의 배포는 순차 처리하지만, 그것만으로 작업 실행을 직렬화하지 않는다. 작업 중복 실행 정책은 D-03에서 정한다. |
| BS-05 | 구체화 | 버전 전환 경계의 일관성 | 동시 배포·수동 실행·스케줄 트리거에도 각 실행이 사용하는 버전은 하나로 확정되어 이력에 남는다. 실행 도중 설정 편집이나 새 배포로 바뀌지 않는다. |
| BS-06 | 구체화 | 배포 취소와 작업 취소 분리 | 배치 배포 취소·버전 복구가 이미 실행 중인 작업을 종료하지 않는다. 활성화 전후의 배포 취소 처리와 작업 취소 지원 여부는 D-04에서 정한다. |
| BS-07 | 구체화 | 제어 컴포넌트 장애 중 작업 보호 | 제어 프로세스가 재시작되어도 실행 중인 작업을 이어서 관측한다. 실제 실행 결과를 알 수 없으면 성공으로 기록하거나 같은 업무를 무조건 재실행하지 않는다. |
| BS-08 | 제안 | 중복 트리거 식별 | 수동 요청 재시도나 같은 스케줄 이벤트 재전달이 하나의 논리적 실행으로 식별된다. 업무의 외부 부작용까지 자동으로 정확히 한 번 처리된다고 보장하지 않는다. |
| BS-09 | 구체화 | 실행 주체와 결과 보존 | 실행 주체 유형, 사용자 또는 스케줄러 식별자, 실행 ID, 버전, 시간, 결과를 남긴다. 스케줄러 교체 후에도 같은 기준으로 조회할 수 있다. |
| BS-10 | 구체화 | 실행 중 자원과 기록 보호 | 배포 버전 변경이나 보관 기간 만료로 실행 중인 작업과 추적에 필요한 기록을 삭제하지 않는다. 공유 DB·볼륨도 자동 정리 대상에 포함하지 않는다. |

### 4.3 배치 실행 이력 보관 정책

| ID | 구분 | 항목 | 요구사항 또는 결정할 내용 |
|---|---|---|---|
| BH-01 | 요청 | 기본 보관 | 최신 실행 이력 10건은 기본적으로 모두 남긴다. |
| BH-02 | 요청 | 옵션 활성화 | 실행 이력 저장 옵션을 켜면 한 달까지 보관한다. |
| BH-03 | 미정 | 보관 단위·최신 기준 | 앱별 10건인지 전체 10건인지, 요청·시작·종료 중 어떤 시각으로 정렬하는지 결정한다. |
| BH-04 | 미정 | 한 달과 최소 10건의 우선순위 | 한 달보다 오래된 기록을 포함해야 10건이 되는 경우의 정책과, 한 달이 달력상 한 달인지 30일인지 결정한다. |
| BH-05 | 미정 | 보관 데이터와 옵션 변경 | 메타데이터·로그·결과물 중 대상 범위와 옵션 변경 시 기존 기록에 적용할 규칙을 결정한다. 이미 삭제한 기록이 복원된다고 안내하지 않는다. |

**보관 정책 제안(미확정):** 앱별로 진행 중인 실행 기록은 보호하고, 완료된 실행을 종료 시각으로 정렬한다.
기본은 최신 10건, 옵션을 켜면 `최신 10건 ∪ 최근 30일 완료 기록`을 보관한다.
이 제안은 최소 10건 보관을 우선하므로 30일보다 오래된 기록이 남을 수 있다. 사용자의 “한 달까지” 의도와 일치하는지 확정해야 한다.

## 5. 구현 전에 결정할 정책

| ID | 대상 | 결정할 내용 |
|---|---|---|
| D-01 | 배치 스케줄러 | Jenkins / Airflow / Kubernetes CronJob 선택, 스케줄 등록·변경·중지의 관리 주체와 시간대. |
| D-02 | 대기 중인 배치 실행 | 요청 접수 시 버전을 고정할지 실제 시작 시 활성 버전을 선택할지. 제안은 접수 시 고정이며, 이 경우 v2 전환 후에도 먼저 접수한 대기 작업은 v1으로 시작할 수 있다. |
| D-03 | 배치 실행 정책 | 같은 앱의 실행 겹침을 허용·대기·건너뛰기 중 무엇으로 처리할지, 재시도·timeout 정책. 기존 작업을 교체 종료하는 정책은 v1 보호 요구와 충돌한다. |
| D-04 | 배치 취소 | 활성화 전 취소, 활성화 완료와 취소가 경합할 때의 결과, 이전 버전 복구 방식. 개별 작업의 강제 취소는 별도 기능으로 채택할지 결정한다. |
| D-05 | 배치 이력 | BH-03~05의 단위·기간·최소 건수 우선순위·보관 범위·옵션 전환 규칙. |
| D-06 | 배포 이력 | 웹·배치 배포 이력 보관 기간과 삭제 정책. 배치 실행 이력의 한 달 규칙을 자동 적용하지 않는다. |
| D-07 | 접근 권한 | namespace·앱별 조회·설정 편집·배포 신청·타인 요청 취소·복구 조치 권한과 관리자 범위. |
| D-08 | 웹 전략 | API 값은 `ROLLING`, `BLUE_GREEN`, `CANARY`로 확정한다. 카나리 노출 방식, 단계·대기 시간·승격 조건·수동 승인 여부, 롤링 가용성 기준, 블루그린 전환과 이전 버전 유지 기간은 실행기 구현에서 확정한다. |
| D-09 | HA·복구 목표 | 저장소를 포함한 단일 프로세스·단일 물리 노드 장애 보장을 기준안으로 검토한다. 장애 시 허용 중단 시간·데이터 손실 범위, 네트워크 분할·전체 클러스터 장애 대응 범위를 확정한다. 같은 호스트의 여러 Kind 노드는 물리 노드 HA 검증이 아니다. |
| D-10 | 성능·규모 | 동시 사용자·앱·대기 요청 수, 처리 공정성, 목록·신청 응답 시간과 화면 상태 갱신 지연의 목표. |
| D-11 | 웹 실패 처리 | 전략별 실패 판정, timeout, 자동 롤백 여부와 복구 한계, 운영자 개입 조건. 진행 중 취소의 이전 버전 복구는 필수다. |

## 6. 테스트 가능한 완료 단위

각 단위는 화면 또는 API로 재현할 수 있어야 한다. 실제 배포·복구 검증은 Kubernetes에서 수행하며 모의 실행 성공으로 대체하지 않는다.
현재 단일 애플리케이션 단계에서는 영속 큐·배포·취소·재시작 복구를 검증한다.
아래 시나리오 중 다른 인스턴스로의 인계, 다중 처리자 경쟁, 노드 장애와 저장소 HA는 후속 HA 구성의 인수 항목으로 남긴다.

| 단위 | 시나리오 | 통과 조건 |
|---|---|---|
| T-01 공통 신청·큐 | 여러 사용자가 앱 A에 연속 신청하고 앱 B에도 신청. A의 대기 요청 하나 취소 | 설정 저장만으로 실행되지 않음. A는 순서대로 하나씩 처리되고 B는 병렬 진행. 취소한 대기 요청은 실행되지 않음. |
| T-02 웹 전략·이력 | 롤링·블루그린·카나리로 실제 이미지/config 배포 | 각 전략의 전환을 관측하고 HTTP 요청으로 확인. 신청자·버전·시간·결과가 이력과 일치. |
| T-03 웹 취소·실패 | 진행 중 취소, 첫 배포 취소, 블루그린 자원 부족, 롤백 timeout | 이전 정상 버전 또는 미배포 상태로 복구. 부족 원인 표시. 복구 미완료 시 완료로 표시하지 않고 같은 앱 후속 Run 차단. |
| T-04 장애 인계 | 요청 접수 직후 및 Kubernetes 변경 직후 프로세스 종료, 이전 처리자 지연 명령 재전달 | 접수한 Run 보존, 실제 상태 대조 후 인계, 중복 배포와 이전 처리자의 덮어쓰기 없음. |
| T-05 orphan·정리 | 기록 없는 리소스, 부분 생성, 이름 재사용, API timeout, 정리 도중 재시작 | 탐지·분류·근거 표시. 확인 가능한 대상만 복구/정리. 현재 서비스·롤백 대상·실행 중 배치·공유 자원 보호. |
| T-06 배치 배포 | 긴 v1 작업 실행 중 v2 배포 후 새 작업 요청 | 기존 작업은 v1으로 완료, 새 요청은 v2로 실행. 배포 자체는 작업을 실행하지 않음. 배포 이력과 실행 이력 분리. |
| T-07 배치 실행 이력 | 수동·스케줄 실행을 섞고 보관 경계를 넘는 기록 준비 | 주체·버전·결과를 구분. 기본 최신 10건 보관. 옵션 활성화 시 합의한 한 달 정책과 최소 건수·진행 중 기록 보호 검증. |
| T-08 권한·운영 | 권한 없는 대상 조회·변경, 저장소 장애·복원, 여러 API/실행기 운용 | 서버에서 접근 차단. 합의한 HA 범위에서 접수 기록과 기존 워크로드 보호. 복원 후 대조 없이 과거 작업을 재생하지 않음. |

## 7. 추가 기능 후보

다음은 기본 동작을 검증한 뒤 검토한다. 현재 필수 범위에 자동 포함하지 않는다.

- 배포 승인, 예약 배포, 변경 금지 시간대.
- 배포·복구 결과 알림과 외부 CI 연동.
- 이미지 빌드·push, 레지스트리 이미지 검색.
- 배치 실행 로그 검색·내보내기, 결과물 다운로드.
- 자원 사전 점검과 예상 소요량 안내. 사전 점검 통과가 실제 배포 성공을 보장하지는 않는다.

## 8. 관련 설계

- [장애 인계·orphan 복구 설계](docs/architecture/deployment-control-v2.md)
- [로그인 보존 및 재구축 계획](docs/architecture/rebuild-plan.md)
- [이전 요구사항 정리](docs/requirements/deployment-requirements-v2.md)

기존 문서의 기술 선택·단계 구분은 설계 참고 자료다. 요구사항을 변경하거나 미정 정책을 확정하면 이 문서도 함께 갱신한다.
